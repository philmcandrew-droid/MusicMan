# Copy to keystore.properties and fill in values after creating your release keystore.
# Never commit keystore.properties or *.keystore to version control.

storeFile=musicman-release.keystore
keyAlias=musicman
storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
